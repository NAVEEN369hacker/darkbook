{
  "name": "ghostline-server",
  "version": "0.1.0",
  "description": "Ghostline identity server (basic level) — DID + daily UID rotation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "supabase": "^2.116.0"
  }
}
